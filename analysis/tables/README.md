# Tables directory

This directory is intentionally empty. The manuscript's
only table (simulation results, Table 1) is generated
inline by `analysis/report/report.Rmd` (`summary-table`
chunk) via `kable()` and embedded directly into the
rendered report, not written as a standalone file to this
directory.
