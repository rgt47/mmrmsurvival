# Figures directory

This directory is intentionally empty. All figures for this
project are generated inline by `analysis/report/report.Rmd`
via knitr chunks (`power-comparison`, `density-plot`) and
embedded directly into the rendered report through the
knitr cache (`analysis/report/cache/`,
`analysis/report/report_files/`), not written as standalone
image files to this directory.
